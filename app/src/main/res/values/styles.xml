<resources>

    <style name="WeekDayTextStyle">
        <item name="android:textSize">@dimen/size_text_week_days</item>
        <item name="android:textColor">@color/selector_day_of_week</item>
        <item name="android:layout_marginLeft">@dimen/padding_week_days</item>
        <item name="android:layout_marginRight">@dimen/padding_week_days</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="LogTimeTextStyle" parent="WeekDayTextStyle">
        <item name="android:textColor">@color/text_common_blue</item>
    </style>
</resources>
